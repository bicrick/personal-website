To start locally run:

npm start from root

Only deploy when I ask. When i ask run this:

npm run deploy to push to vercel

Do not run 'echo' commands after generation to say a implementation is ready.